# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Logs y temporales
*.log
*.tmp

# Dependencias (por si en algún momento se suma npm/composer)
node_modules/
vendor/

# Backups locales de la base de datos con datos reales de usuarios
# (el dump de estructura en /database sí se sube, ese no se toca)
*.sql.bak

# Scripts de migración de un solo uso (no deben quedar en producción)
src/api/migrar_*.php
